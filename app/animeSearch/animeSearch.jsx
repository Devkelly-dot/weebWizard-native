import { SafeAreaView } from "react-native-safe-area-context";
import AnimeSearchScreen from "../screens/animeSearch/animeSearchScreen";

export default function AnimeSearchTab() {
    return (
        <SafeAreaView>
            <AnimeSearchScreen/>
        </SafeAreaView>
    )
}