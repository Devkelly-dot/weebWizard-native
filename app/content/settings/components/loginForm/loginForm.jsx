import { View } from "react-native";
import Form from "../../../../components/form/form";

export default function LoginForm() {
    const fields = [

    ]
    return (
        <View>
            <Form
                fields={fields}
            />
        </View>
    )
}